# Agent 5: UI & Telemetry Developer
**Role:** Frontend & UX Designer
**Responsibilities:** - Building the local web interface using Python's native `http.server`.
- Implementing the "Plum Dark Mode" aesthetic.
- Developing real-time telemetry endpoints (`/stats`, `/history`) to visualize indexing progress, queue depth, and blocked requests.
- Ensuring the UI remains responsive while background crawling threads are active.